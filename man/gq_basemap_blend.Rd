% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gq_basemap_blend.R
\name{gq_basemap_blend}
\alias{gq_basemap_blend}
\title{Multiply a basemap by a relief layer}
\usage{
gq_basemap_blend(
  base,
  relief,
  method = c("gamma", "weight"),
  gamma = 0.5,
  weight = 0.35,
  relief_max = NULL,
  as_stars = TRUE
)
}
\arguments{
\item{base}{A \code{SpatRaster} basemap, values in 0-255. RGB is fine.}

\item{relief}{A \code{SpatRaster} relief or hillshade. Multi-band input is
averaged to one band. Values in 0-255, or 0-1 — see \code{relief_max}.}

\item{method}{\code{"gamma"} or \code{"weight"}. See details.}

\item{gamma}{Exponent for \code{method = "gamma"}. Lower is lighter.}

\item{weight}{Maximum darkening for \code{method = "weight"}, in \verb{[0, 1]}.}

\item{relief_max}{Value corresponding to full brightness in \code{relief}.
\code{terra::shade()} returns 0-1; a tile service returns 0-255. Defaults to
detecting which from the data.}

\item{as_stars}{Convert the result for \code{tmap::tm_rgb()}, which takes a
\code{stars} object rather than a \code{SpatRaster}.}
}
\value{
A \code{SpatRaster}, or a \code{stars} object when \code{as_stars = TRUE}.
}
\description{
Shades a flat raster basemap with terrain so relief reads as backdrop rather
than as subject. Both inputs are \code{terra} rasters; the relief is resampled onto
the basemap grid, reduced to one band if it has several, and multiplied
through.
}
\section{Two operators, deliberately}{


\code{"gamma"} raises the normalised relief to a power before multiplying:
\code{base * relief^gamma}. Lower gamma means lighter shading. This suits a relief
\emph{tile service}, whose values already span the full range.

\code{"weight"} pulls the relief toward 1 linearly: \code{base * (1 - w * (1 - relief))}.
\code{w} is the maximum darkening, so \code{w = 0.35} can never remove more than 35\% of
the basemap's brightness. This suits a hillshade derived from a DEM, which
can be far more contrasty than a tile service and at full strength turns the
map into a greyscale DEM with lines on it.

Both are in use across the reporting repos, and the two were believed to be
the same operator with different inputs — one implementation says so in its
own documentation. They are not: at the same nominal strength they produce
visibly different maps. Naming them is the point.
}

\examples{
if (requireNamespace("terra", quietly = TRUE)) {
  base <- terra::rast(nrows = 4, ncols = 4, vals = 200)
  relief <- terra::rast(nrows = 4, ncols = 4, vals = 128)

  # gamma barely darkens a mid-grey relief
  round(terra::values(gq_basemap_blend(base, relief, as_stars = FALSE))[1])

  # weight caps how much can ever be removed
  round(terra::values(
    gq_basemap_blend(base, relief, method = "weight", as_stars = FALSE)
  )[1])
}

}
