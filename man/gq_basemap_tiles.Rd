% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gq_basemap_blend.R
\name{gq_basemap_tiles}
\alias{gq_basemap_tiles}
\title{Fetch basemap tiles for a bounding box}
\usage{
gq_basemap_tiles(
  bbox,
  provider = "CartoDB.PositronNoLabels",
  zoom = 12,
  pad = 0.1,
  crs = 3005
)
}
\arguments{
\item{bbox}{A \code{bbox}. Reprojected to EPSG:4326 for the request.}

\item{provider}{A \code{maptiles} provider name.}

\item{zoom}{Tile zoom level.}

\item{pad}{Fraction of the bbox width to buffer before requesting.}

\item{crs}{CRS to reproject tiles into. \code{NULL} keeps Web Mercator.}
}
\value{
A \code{SpatRaster}, or \code{NULL} if the request failed.
}
\description{
Thin wrapper over \code{\link[maptiles:get_tiles]{maptiles::get_tiles()}} that adds the padding a projected
map needs and reprojects the result.
}
\details{
Tiles arrive in Web Mercator. Reprojecting them to a projected CRS turns the
covered area into a slightly rotated quadrilateral, so requesting exactly the
frame leaves empty white wedges in the corners. \code{pad} requests a larger area
so the rotated coverage still spans the frame. The rotation is a fixed
angular effect, so it is proportionally \emph{more} significant at small extents.
}
\examples{
\dontshow{if (interactive() && requireNamespace("maptiles", quietly = TRUE)) withAutoprint(\{ # examplesIf}
bb <- sf::st_bbox(
  c(xmin = 1e6, ymin = 9e5, xmax = 1.02e6, ymax = 9.2e5),
  crs = 3005
)
tiles <- gq_basemap_tiles(bb, zoom = 11)
\dontshow{\}) # examplesIf}
}
