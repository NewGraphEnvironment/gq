% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gq_groups.R
\name{gq_group_layers}
\alias{gq_group_layers}
\title{Get layers in a group}
\usage{
gq_group_layers(group, registry = NULL)
}
\arguments{
\item{group}{Character. Group name (e.g., \code{"Basemap"}, \code{"Crossings"}).}

\item{registry}{Optional registry list (from \code{\link[=gq_reg_main]{gq_reg_main()}}). If provided,
\code{source_layer} and \code{type} columns are joined from the style registry.}
}
\value{
A data.frame with columns: group, subgroup, layer_key, order,
and optionally source_layer and type. Returns empty data.frame if group
not found.
}
\description{
Returns all layers belonging to a group, including any nested subgroups.
Layers are ordered by z-order within the group.
}
\examples{
gq_group_layers("Basemap")
gq_group_layers("Streams")

# With source_layer info
reg <- gq_reg_main()
gq_group_layers("Crossings", registry = reg)

}
