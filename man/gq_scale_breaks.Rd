% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gq_scale_breaks.R
\name{gq_scale_breaks}
\alias{gq_scale_breaks}
\title{Scale bar breaks appropriate to a map extent}
\usage{
gq_scale_breaks(bbox, n = 3, share = 0.35)
}
\arguments{
\item{bbox}{A \code{bbox} in a projected CRS with metre units. A geographic bbox
has degree spans, so the returned breaks would not be kilometres.}

\item{n}{Number of intervals.}

\item{share}{Fraction of the frame width the whole bar should occupy.}
}
\value{
A numeric vector of length \code{n + 1}, starting at 0, in kilometres.
}
\description{
Returns breaks in kilometres, rounded to a 1/2/5 sequence so the bar reads
cleanly at any extent.
}
\details{
The whole bar is sized to \code{share} of the frame width — \emph{not} \code{share} per
interval. Sizing per interval overruns the frame, at which point tmap reports
"not all scale bar breaks could be plotted" and then silently drops every
label but the last, which looks like a styling problem rather than a sizing
one.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE)) withAutoprint(\{ # examplesIf}
bb <- sf::st_bbox(
  c(xmin = 1e6, ymin = 9e5, xmax = 1.1e6, ymax = 1e6),
  crs = 3005
)
gq_scale_breaks(bb)

# a wider extent steps up to the next round number
wide <- bb
wide[["xmax"]] <- wide[["xmin"]] + 400000
gq_scale_breaks(wide)
\dontshow{\}) # examplesIf}
}
