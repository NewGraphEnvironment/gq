% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gq_style.R
\name{gq_style}
\alias{gq_style}
\title{Get backend-agnostic style for a registry layer}
\usage{
gq_style(layer_or_reg, name = NULL)
}
\arguments{
\item{layer_or_reg}{Either a layer entry from the registry
(e.g., \code{reg$layers$lake}) or a full registry list when using name-based
lookup.}

\item{name}{Optional layer name for name-based lookup. Accepts
\code{name_qgis_snake} (e.g., \code{"lake"}) or \code{name_qgis} (e.g.,
\code{"Crossings - PSCIS assessment"}). Normalized to match registry keys.}
}
\value{
A named list with \code{type} and style properties. For simple layers:
\code{fill}, \code{stroke}, \code{mark} as applicable. For classified layers: adds
\code{classification} with \code{field}, \code{values} (named color vector), \code{labels},
and per-class \code{widths}/\code{radii} when available.
}
\description{
Resolves a layer by name and returns a plain list of style properties.
No backend-specific objects (tmap, mapgl, etc.) — just colors, widths,
shapes, classification fields, and values.
}
\examples{
path <- system.file("examples", "mini_registry.json", package = "gq")
reg <- gq_registry_read(path)

# Simple polygon
gq_style(reg, "lake")

# Simple line
gq_style(reg, "stream")

# Classified line — includes field, values, labels
gq_style(reg, "road")

# Object-based (backwards compatible)
gq_style(reg$layers$lake)

}
