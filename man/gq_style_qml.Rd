% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gq_style_qml.R
\name{gq_style_qml}
\alias{gq_style_qml}
\title{Get the QGIS-native QML style for a layer}
\usage{
gq_style_qml(layer_key, template = NULL)
}
\arguments{
\item{layer_key}{Layer key, as used by \code{\link[=gq_groups]{gq_groups()}} and the registry (e.g.
\code{"lake"}). See \code{\link[=gq_groups]{gq_groups()}} for the roster.}

\item{template}{Optional project template (e.g. \code{"bcfishpass_mobile"}).
When supplied, a template-specific override wins over the shared style.
When \code{NULL} (default) the shared style is returned.}
}
\value{
A length-one character path to a \code{.qml} file.
}
\description{
Returns the path to a layer's QML — the QGIS-native form of its symbology,
complete in a way the registry is not. \code{gq_reg_main()} and the
\code{gq_tmap_style()} / \code{gq_mapgl_style()} translators model roughly 20 symbol
properties and a single symbol layer, because that is what tmap and mapgl can
render. A QML carries everything QGIS authored: multi-layer symbols, casing
and overlay, labelling, per-class dash. Use the registry for tmap and mapgl,
and this for anything that speaks to QGIS — Desktop, Mergin field projects,
QGIS Server / QWC2, or a \code{layer_styles} table.
}
\details{
Unlike every other gq export, this returns a \strong{file path} rather than a list
or a data frame. The QML is shipped as-is, byte-for-byte what QGIS wrote, so
handing back a path lets the caller copy it, read it, or write it into a
\code{layer_styles} row without gq re-serializing it and introducing drift.

Styles are shared across project templates unless a template genuinely
diverges — measured upstream, 3 layers of 53 do. Passing \code{template} returns
that template's override when one exists and the shared style otherwise, so
naming a template is always safe.
}
\examples{
# the QGIS-native style for lakes
qml <- gq_style_qml("lake")
basename(qml)

# a QML is a full QGIS style document, not a property digest
doc <- xml2::read_xml(qml)
xml2::xml_name(xml2::xml_root(doc))

# three layers differ between templates; the rest are shared
basename(dirname(gq_style_qml("land_ownership", "bcfishpass_mobile")))
basename(dirname(gq_style_qml("land_ownership", "bcrestoration_mobile")))

}
\seealso{
\code{\link[=gq_reg_main]{gq_reg_main()}} for the cross-backend registry, \code{\link[=gq_tmap_style]{gq_tmap_style()}}
and \code{\link[=gq_mapgl_style]{gq_mapgl_style()}} for the tmap and mapgl translations.
}
