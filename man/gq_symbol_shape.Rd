% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gq_symbol.R
\name{gq_symbol_shape}
\alias{gq_symbol_shape}
\title{Translate a registry symbol shape to a rendering target}
\usage{
gq_symbol_shape(shape, target = c("tmap", "mapgl"))
}
\arguments{
\item{shape}{Registry shape string, or \code{NULL}.}

\item{target}{One of \code{"tmap"} or \code{"mapgl"}.}
}
\value{
For \code{"tmap"}, an integer \code{pch}. For \code{"mapgl"}, \code{NULL}. \code{NULL} in
gives \code{NULL} out.
}
\description{
The registry carries a \code{shape} on every mark — \code{circle}, \code{square}, \code{star} and
\code{triangle} are the complete vocabulary across all three registry files. Until
#16 no renderer consumed it, so maps hardcoded their own marker codes.
}
\details{
\code{star} maps to \code{pch = 8}, which draws a star but takes \strong{no fill} — R's
fillable symbols (21–25) are circle, square, diamond, triangle-up and
triangle-down, and none is a star. Substituting a filled circle would silently
discard the distinction that made the layer a star in QGIS, so the unfillable
code is returned instead and the caller sees an unfilled star.

\code{mapgl} returns \code{NULL} for every shape. A MapLibre \code{circle} layer has no shape
concept at all; anything else requires a \code{symbol} layer with an icon sprite,
which the registry has no source data for. \code{NULL} says "not expressible here"
rather than handing back a \code{pch} number the renderer cannot read.
}
\examples{
gq_symbol_shape("circle", "tmap")
gq_symbol_shape("triangle", "tmap")
gq_symbol_shape("star", "tmap")     # 8 -- drawn unfilled

}
\seealso{
\code{\link[=gq_symbol_size]{gq_symbol_size()}}
}
