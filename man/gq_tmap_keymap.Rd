% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gq_tmap_keymap.R
\name{gq_tmap_keymap}
\alias{gq_tmap_keymap}
\title{Build an overview keymap and the viewport to place it in}
\usage{
gq_tmap_keymap(
  aoi,
  context,
  reg = NULL,
  aoi_layer = "watershed_group_boundary",
  context_layer = "provincial_park",
  corner = c("bottomright", "bottomleft", "topright", "topleft"),
  width = 0.25,
  height = 0.22,
  margin = 0.03
)
}
\arguments{
\item{aoi}{The area of interest — the thing being located.}

\item{context}{Wider context, drawn beneath. A province or basin outline.}

\item{reg}{A registry for the fill and stroke colours. Defaults to
\code{\link[=gq_reg_main]{gq_reg_main()}}. The copies this replaces all hardcode hex here, including
one that takes a registry argument and then does not use it.}

\item{aoi_layer, context_layer}{Registry keys supplying the two styles.}

\item{corner}{Which corner to place the inset in.}

\item{width, height}{Size as a fraction of the device.}

\item{margin}{Gap between the inset and the frame edge, as a fraction of the
device. The four-corner convention wants this equal for every element.}
}
\value{
A list with \code{map} (a \code{tmap} object) and \code{viewport} (a
\code{\link[grid:viewport]{grid::viewport()}}). Print \code{map} into \code{viewport} after the main map.
}
\description{
A keymap is a small inset showing where the mapped area sits in a wider
region. This returns the inset as its own \code{tmap} object plus a
\code{\link[grid:viewport]{grid::viewport()}} positioning it, so the caller prints the main map and then
the keymap over it.
}
\section{Why a viewport rather than a component}{
tmap has no inset-map component, so every implementation of this prints a
second \code{tmap} object into a viewport. The placement arithmetic is where they
diverge — each of the copies this replaces hardcodes a different pair of
centre coordinates, and a viewport's \code{x}/\code{y} are its \emph{centre}, so the numbers
are not the margin they look like. \code{corner} and \code{margin} compute them, so
moving a keymap between corners does not mean re-deriving them by eye.
}

\examples{
\dontshow{if (requireNamespace("tmap", quietly = TRUE) && requireNamespace("sf", quietly = TRUE)) withAutoprint(\{ # examplesIf}
aoi <- sf::st_as_sfc(sf::st_bbox(
  c(xmin = 1.0e6, ymin = 9.0e5, xmax = 1.1e6, ymax = 1.0e6), crs = 3005
))
context <- sf::st_as_sfc(sf::st_bbox(
  c(xmin = 0.5e6, ymin = 5.0e5, xmax = 1.8e6, ymax = 1.4e6), crs = 3005
))

km <- gq_tmap_keymap(aoi, context)
class(km$map)

# bottom-right by default, and the viewport centre reflects the margin
round(c(km$viewport$x, km$viewport$y), 3)
\dontshow{\}) # examplesIf}
}
