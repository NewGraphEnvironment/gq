% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gq_tmap_legend.R
\name{gq_tmap_legend}
\alias{gq_tmap_legend}
\title{Build tmap legend arguments from registry layers}
\usage{
gq_tmap_legend(reg, layers, present = NULL, field = NULL, titles = NULL, ...)
}
\arguments{
\item{reg}{A registry, as from \code{\link[=gq_reg_main]{gq_reg_main()}}.}

\item{layers}{Layer keys. An unnamed character vector uses each layer's
title-cased key as its label; a named vector or list uses the names as
labels (\code{c("Lake" = "lake")}).}

\item{present}{Optional named list restricting classified layers to the
values actually in the data, e.g. \code{list(roads_dra = unique(x$road_type))}.
A legend naming classes the map does not draw is a common and quiet error.}

\item{field}{Optional named character vector overriding the classification
field per layer, matching \code{\link[=gq_style]{gq_style()}}'s \code{field}.}

\item{titles}{Optional named character vector of legend titles per geometry
type, e.g. \code{c(symbols = "Crossings")}.}

\item{...}{Extra arguments merged into every returned list — \code{z},
\code{group_id}, \code{orientation}, \code{position} and so on.}
}
\value{
A named list of argument lists, one per geometry type present
(\code{polygons}, \code{lines}, \code{symbols}). Each is ready for
\code{do.call(tmap::tm_add_legend, x)}.
}
\description{
Turns a set of layer keys into argument lists for \code{\link[tmap:tm_add_legend]{tmap::tm_add_legend()}},
one per geometry type, pulling every colour, width, dash and symbol from the
registry rather than from parallel vectors typed by hand.
}
\details{
Classified layers expand to one entry per class; simple layers contribute a
single entry. Both kinds can appear in the same call and are merged into the
right geometry group.
}
\section{What this does not do}{
Layout. tmap 4.4 handles ordering (\code{z}), grouping (\code{group_id}), stacking and
framing (\code{\link[tmap:tm_components]{tmap::tm_components()}}), and bulk placement
(\verb{tm_place_legends_*()}) — all of it better than a wrapper could, because it
can see the whole map object. \code{z} and \code{group_id} are passed straight through
so those facilities keep working.

What tmap cannot do is read a style registry, because it has no concept of
one. That translation is the whole of this function.
}

\examples{
reg <- gq_reg_main()

# mixed geometry types partition automatically
leg <- gq_tmap_legend(reg, c("lake", "railway"))
names(leg)
leg$polygons$labels

# a classified layer expands to one entry per class
roads <- gq_tmap_legend(reg, "roads_dra")
length(roads$lines$labels)

# ... and can be cut down to the classes the data actually contains
some <- gq_tmap_legend(reg, "roads_dra",
                       present = list(roads_dra = c("RH1", "RA1")))
length(some$lines$labels)

# labels come from the names when supplied
gq_tmap_legend(reg, c("Waterbody" = "lake"))$polygons$labels

}
\seealso{
\code{\link[=gq_tmap_style]{gq_tmap_style()}} for drawing the layers themselves,
\code{\link[tmap:tm_components]{tmap::tm_components()}} for arranging the results.
}
